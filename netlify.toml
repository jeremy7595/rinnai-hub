# Edge functions for the durable PDF archive (Netlify Blobs).
# Declared explicitly so Netlify bundles and routes them.

[[edge_functions]]
  path = "/doc/*"
  function = "doc"

[[edge_functions]]
  path = "/archive-status"
  function = "archive-status"
